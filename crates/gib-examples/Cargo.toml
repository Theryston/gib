[package]
name = "gib-examples"
version = "0.1.0"
edition = "2024"
publish = false

[features]
default = []
s3 = ["gib-sdk/s3"]
webdav = ["gib-sdk/webdav"]

[lib]
name = "gib_examples"
path = "src/lib.rs"

[dependencies]
gib-sdk = { path = "../gib-sdk", default-features = false }
